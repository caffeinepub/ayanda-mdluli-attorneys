{
  "kind": "build_request",
  "title": "Replace navbar logo image with text brand",
  "projectName": "Ayanda Mdluli Attorneys",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "In `frontend/src/components/home/HomeNavbar.tsx`, replace the navbar logo `<img>` element (XPath: `/html[1]/body[1]/div[1]/nav[1]/div[1]/div[1]/button[1]/div[1]/img[1]`) with a text node that reads exactly: `Ayanda Mdluli Atttorneys` (remove the image entirely so no broken image icon can appear).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-4",
          "recent-6"
        ],
        "quotes": [
          "Remove this image and replace with the text Ayanda Mdluli Atttorneys",
          "Build"
        ]
      },
      "acceptanceCriteria": [
        "The logo area in the navbar renders the text `Ayanda Mdluli Atttorneys` instead of an image.",
        "No `<img>` element exists for the navbar logo in `HomeNavbar.tsx` after the change.",
        "The navbar no longer shows a broken image icon in the logo position on reload.",
        "The text remains inside the existing logo container so the navbar height and alignment remain consistent across breakpoints."
      ]
    }
  ],
  "constraints": [
    "Apply changes ONLY to the selected element (the logo `<img>` in `frontend/src/components/home/HomeNavbar.tsx`) and do not modify other navbar elements or behavior.",
    "Do not edit any files under `frontend/src/components/ui` or any paths listed as immutable in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "Do not add/modify backend canister logic.",
    "Do not add or fix any image assets (the logo is being replaced by text).",
    "Do not change routing, navigation links, or other navbar layout outside of the selected `<img>` element."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}